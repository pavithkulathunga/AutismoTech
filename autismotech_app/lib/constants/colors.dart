import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFF673BB6);
  static const diagnosis = Colors.lightBlue;
  static const Attention = Colors.orange;
  static const Emotion = Color.from(alpha: 1, red: 0.298, green: 0.686, blue: 0.314);
  static const Progress = Color(0xFF122975);
}
